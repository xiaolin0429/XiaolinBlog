FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 安装系统依赖
RUN apk add --no-cache \
    git \
    curl \
    vim

# 复制package文件
COPY package*.json ./

# 安装依赖（包括开发依赖）
RUN npm ci

# 复制应用代码
COPY . .

# 暴露端口
EXPOSE 3000

# 开发环境启动命令（支持热重载）
CMD ["npm", "run", "dev"]